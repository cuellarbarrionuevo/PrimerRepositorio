para inicir git: 

git init
git config --global user.name "cuellar"
git config --global user.email juanjosecuellarherrera@gmail.com
git config --global core.editor "code --wait"
git config --global -e
git config --global core.autocrlf true

pasara a la fase stage
git add nombre_fichero

pasar a la fase commit
git commit -m "que hemos hecho"

gir push -u origin main